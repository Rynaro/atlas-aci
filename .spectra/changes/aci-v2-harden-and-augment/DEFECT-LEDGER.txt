# Verified defect ledger — atlas-aci v2.0.0
# Generated by the orchestrator from git, because IDG has no Bash and cannot read commits.
# Every line below is a real commit on feat/v2-p1-edges or feat/v2-p0-harden.

## Base commit for all scout-report line anchors: f56a78e (tag v0.4.0)
## Ground truth at that commit:
  codegraph.py:419 = "SELECT * FROM refs WHERE callee_name = ? LIMIT 200", (name,)
  config.py at f56a78e has NO max_bound_field_elements — P0 introduced it at 45b7210:config.py:54
  F-1 is therefore a collision P0 CREATED: its new cap (200) met the pre-existing hardcoded LIMIT 200.
  'rationale' occurrences in codegraph.py at f56a78e: 0
  (the same LIMIT statement sits at :647 after P0 inserted ~230 lines above it)

## scout-atlas-aci.md Deliverable 3 numbered items: 17
## item 12 reads:
  12. **`.tsx`/`.go`/`.rs`/`.java` are recognized extensions with no matching query, so they silently 

## Fix commits, oldest first (git log --oneline f56a78e..HEAD, reversed):
  45b7210 feat(v2-p0): harden-first gate — central bounds, epoch DB substrate, CI, doc honesty
  4ab56c1 fix(v2-p0): separate the absolute byte ceiling from the per-tool byte cap
  7524ffc fix(v2-p0): F-1/F-2 — SQL LIMIT/central-cap boundary collision (checker finding)
  f7b74fd fix(v2-p0): F-6/F-7 — unify truncation vocabularies, per-field returned_count
  7cff938 fix(v2-p0): F-5 — correct the --frozen freshness claim in ci.yml
  3f5b98b fix(v2-p0): AC-H-16 — serve must not crash under a real --read-only :ro mount
  0763ff5 feat(v2-p0): AC-H-16 — land the Docker --read-only :ro smoke test in CI
  76b4427 fix(v2-p0): F-3 — verify + close the "new verb evades harden-gate" gap
  da2ea3d fix(v2-p0): NEW-1 — view_file's F-6 promotion cried wolf on every window
  3717dde fix(v2-p0): NEW-2 — guard the query_limit <-> config-cap wiring
  fd1ed20 fix(v2-p0): NEW-3 — an un-dispatched known verb must raise, not impersonate a stub
  2761cda fix(v2-p0): NEW-1 residual — state the withholding invariant, don't proxy it
  1e3cfd7 fix(v2-p0): harden-gate false positive — comments matched as code (checker third pass)
  1894d2f docs(esl): record the aci-v2-harden-and-augment change folder
  9d2c37d fix(v2-p1): R-1 — close the candidates[] nested sub-field escape; test_dry_run truncated_fields residual
  0cbb021 feat(v2-p1): A1 — materialized edge table, deterministic confidence enum, real subclasses_of
  74e2a08 docs(v2-p1): AC-A1-10 — document graph_query's edge shape and the D4a analysis-graph divergence
  3eba538 fix(v2-p1): BLOCKER — resolve constructor calls to local classes; MAJOR — replace the capitalization proxy with symbol-kind resolution
  ea0eec3 feat(v2-p1): MAJOR — surface unresolved_refs so an empty callers_of/subclasses_of answer is distinguishable from an incomplete one
  3e92167 fix(v2-p1): condition 1 — AC-H-18's named test never existed; create it and wire it into the gate
  513fbba fix(v2-p1): condition 2 (MINOR-1) — close the bug class, not just add mixin
  884502f fix(v2-p1): condition 3 (MINOR-4) — total-order the edge enumeration before A5 needs it
  c4b0b06 fix(v2-p1): condition 4 (MINOR-3) — close the edges.relation vocabulary
  b5c2d64 fix(v2-p1): condition 5 (MAJOR-1) — demote shadowed qualifiers to INFERRED instead of a false EXTRACTED
  8e1e7a8 docs(v2-p1): note the shadowing guard is file-scoped, not scope-scoped
  ce93763 feat(v2-p1): A2 — degree-centrality god nodes over the confident subgraph
  9abffd8 docs(v2-p1): fix a doc-honesty defect — A3's outcome was asserted before the probe ran
  f7cc1db docs(v2-p1): land A1 checker verdict + finalize R-1/R15 scope tracking
  21014c9 feat(v2-p1): A3 — deterministic LPA communities over the confident subgraph
  1353f16 fix(v2-p1): condition MAJOR-1 — harden-gate recomputes the D3a verdict, never greps a label
  e6e263e fix(v2-p1): condition MINOR-1 — god_nodes() double-guards against target=None like communities()
  e71bb06 fix(v2-p1): checker defect 11 — verifier hardcodes the frozen bar, never reads it from the artifact
  61403c0 fix(v2-p1): checker defect 12 — verifier asserts exactly the two pinned repos and seeds
  4045998 fix(v2-p1): checker defect 13 — recompute every modularity Q from a committed graph bundle
  b4e1b02 fix(v2-p1): close the staleness hole — indexer fingerprint + name the terminus
  71bfe2c docs(v2-p1): land RAMZA's scope amendment for the D3a probe machinery
  c6e3f87 fix(v2-p1): wire the probe/classifier self-tests and scripts/ lint into CI
  7a41c5c fix(v2-p1): checker defect 15 — fingerprint under-covered the graph-determining logic
  3b4cb35 feat(v2-p1): A4 — rationale nodes (Ruby -> Python -> JS/TS, D5)
  435f94c fix(v2-p1): close the class in test_every_tool_and_verb_truncates_and_flags_over_cap
  3a6a43d fix(v2-p1): checker defect 17 — indexer fingerprint hashed source text, not behaviour
  1238161 fix(v2-p1): two closures — node-identity uniqueness, LPA re-derivation
  02a3e9e feat(v2-p1): A5 — deterministic sorted-JSONL export + idempotent import (D6)
  5dbc8e5 fix(v2-p1): self-catch — import_jsonl let a truncated line raise a raw traceback
  bf58baf fix(v2-p1): checker verdict A4/A5 — MAJOR-1/MINOR-1/2/3/4 (the nineteenth defect)
  edad2b8 feat(v2-p1): A5 CLI surface — atlas-aci export/import (closes defect 18)
  2ff5723 docs(v2-p1): land A4/A5 checker verdict
  ace7fa9 feat(v2-p1): AC-REL-2 — Spree export-size measurement + CI gate
  4bb6ea2 fix(v2-p1): AC-REL-3 — uv.lock/pyproject.toml version bump to 2.0.0
  1bff5ad feat(v2-p1): AC-REL-2 — warn from atlas-aci export as size approaches GitHub's ceiling
  e945e91 docs(v2-p1): AC-REL-2 documented bound, Migration section, committable export path
  277358f docs(v2-p1): CHANGELOG for v2.0.0
  9b62da2 fix(v2-p1): the twentieth defect — "fails fast" implied startup, it means tool-call time
  b1c04df chore(esl): transition aci-v2-harden-and-augment to verified
